server:
  port: 8081

spring:
  application:
    name: nacos-config-a
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        prefix: nacos-config
        file-extension: yml

